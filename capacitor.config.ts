import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
    appId: 'rossalexandra.ionic-template',
    appName: 'ionic-template',
    webDir: 'build',
    bundledWebRuntime: false
};

export default config;
